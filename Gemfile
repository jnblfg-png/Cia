# Gemfile — pins Fastlane so `bundle exec fastlane beta` is reproducible on the Mac.
# Setup on the Mac:
#   sudo gem install bundler   # if needed
#   bundle install             # installs fastlane into ./vendor or system
#   bundle exec fastlane beta  # run the lane
source "https://rubygems.org"

gem "fastlane"
